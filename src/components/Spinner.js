import React from "react";
import "./style.css";

// Spinner displayed while image is being loaded
const Spinner = () => {
  return <div className="loader">Loading...</div>;
};

export default Spinner;
